
// import { Roles } from "@Rol/domain/Roles"


export const seedData = async () =>{
    
    // const [countRoles, countNivel, countGrado, countModalidad, countArea, countCurso, countPlan] = await Promise.all([
    //     Roles.count()
    // ]);

    // if ([countRoles, countNivel, countGrado, countModalidad, countArea, countCurso, countPlan].some(count => count > 0)) {
    //     return { message: "Data insertada previamente" };
    // }

    // await Promise.all([

    //     Roles.bulkCreate([
    //         {name:"Alumno"},
    //         {name:"Padre"},
    //         {name:"Profesor"},
    //         {name:"Administrador"}
    //     ]),
    
       
    // ])
    
    // return {message: "Seeding completed successfully"}

}